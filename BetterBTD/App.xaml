<Application x:Class="BetterBTD.App"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
              xmlns:vio="http://schemas.lepo.co/wpfui/2022/xaml/violeta"
              xmlns:behavior="clr-namespace:BetterBTD.View.Behavior"
              xmlns:converters="clr-namespace:BetterBTD.Views.Converters"
             StartupUri="MainWindow.xaml">
    <Application.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ui:ThemesDictionary Theme="Dark" />
                <ui:ControlsDictionary />
                <vio:ControlsDictionary />
                <ResourceDictionary Source="/Views/Controls/WpfUi/FaFontIconStyle.xaml" />
                <ResourceDictionary Source="/Views/Controls/Drawer/DrawerStyles.xaml" />
            </ResourceDictionary.MergedDictionaries>

            <BooleanToVisibilityConverter x:Key="BoolToVisibilityConverter" />
            <converters:BooleanToVisibilityRevertConverter x:Key="BooleanToVisibilityRevertConverter" />
            <converters:BooleanToEnableTextConverter x:Key="BooleanToEnableTextConverter" />

            <SolidColorBrush x:Key="TextFillColorPrimaryBrush" Color="#F3F6FB" />
            <SolidColorBrush x:Key="TextFillColorTertiaryBrush" Color="#A7B3C5" />
            <SolidColorBrush x:Key="ApplicationPageBackgroundThemeBrush" Color="#0D111B" />
            <SolidColorBrush x:Key="ControlFillColorSecondaryBrush" Color="#1B2536" />
            <SolidColorBrush x:Key="SubtleFillColorSecondaryBrush" Color="#243247" />
            <SolidColorBrush x:Key="ControlStrokeColorSecondaryBrush" Color="#2E4058" />
            <SolidColorBrush x:Key="AccentFillColorDefaultBrush" Color="#4C8DFF" />
            <SolidColorBrush x:Key="AccentFillColorSecondaryBrush" Color="#334A6E" />
            <SolidColorBrush x:Key="AccentTextFillColorPrimaryBrush" Color="#73A7FF" />

            <Style TargetType="Window">
                <Setter Property="FontFamily" Value="Segoe UI" />
                <Setter Property="FontSize" Value="13" />
                <Setter Property="Foreground" Value="{DynamicResource TextFillColorPrimaryBrush}" />
                <Setter Property="Background" Value="{DynamicResource ApplicationPageBackgroundThemeBrush}" />
            </Style>

            <Style TargetType="UserControl">
                <Setter Property="FontFamily" Value="Segoe UI" />
                <Setter Property="FontSize" Value="13" />
                <Setter Property="Foreground" Value="{DynamicResource TextFillColorPrimaryBrush}" />
                <Setter Property="Background" Value="Transparent" />
            </Style>

            <Style TargetType="Page">
                <Setter Property="FontFamily" Value="Segoe UI" />
                <Setter Property="FontSize" Value="13" />
                <Setter Property="Foreground" Value="{DynamicResource TextFillColorPrimaryBrush}" />
                <Setter Property="Background" Value="Transparent" />
            </Style>

            <Style x:Key="PageRootPanelStyle" TargetType="StackPanel">
                <Setter Property="Margin" Value="42,16,42,12" />
            </Style>

            <Style x:Key="PageTitleTextStyle" TargetType="TextBlock">
                <Setter Property="FontSize" Value="24" />
                <Setter Property="FontWeight" Value="SemiBold" />
                <Setter Property="Foreground" Value="{DynamicResource TextFillColorPrimaryBrush}" />
            </Style>

            <Style x:Key="SectionTitleTextStyle" TargetType="TextBlock">
                <Setter Property="FontSize" Value="18" />
                <Setter Property="FontWeight" Value="SemiBold" />
                <Setter Property="Foreground" Value="{DynamicResource TextFillColorPrimaryBrush}" />
            </Style>

            <Style x:Key="PageDescriptionTextStyle" TargetType="TextBlock">
                <Setter Property="Foreground" Value="{DynamicResource TextFillColorTertiaryBrush}" />
                <Setter Property="TextWrapping" Value="Wrap" />
            </Style>

            <Style x:Key="CardTitleTextStyle" TargetType="TextBlock">
                <Setter Property="FontSize" Value="14" />
                <Setter Property="FontWeight" Value="SemiBold" />
                <Setter Property="Foreground" Value="{DynamicResource TextFillColorPrimaryBrush}" />
            </Style>

            <Style x:Key="CardDescriptionTextStyle" TargetType="TextBlock">
                <Setter Property="Foreground" Value="{DynamicResource TextFillColorTertiaryBrush}" />
                <Setter Property="TextWrapping" Value="Wrap" />
            </Style>

            <Style x:Key="AppCardControlStyle" TargetType="ui:CardControl">
                <Setter Property="Margin" Value="0,0,0,12" />
            </Style>

            <Style x:Key="AppCardExpanderStyle" TargetType="ui:CardExpander">
                <Setter Property="Margin" Value="0,0,0,12" />
                <Setter Property="ContentPadding" Value="0" />
            </Style>

            <Style x:Key="StatusPillStyle" TargetType="Border">
                <Setter Property="CornerRadius" Value="999" />
                <Setter Property="Padding" Value="10,4" />
                <Setter Property="Background" Value="{DynamicResource ControlFillColorSecondaryBrush}" />
                <Setter Property="BorderBrush" Value="{DynamicResource ControlStrokeColorSecondaryBrush}" />
                <Setter Property="BorderThickness" Value="1" />
                <Setter Property="Margin" Value="0,0,8,0" />
            </Style>

            <Style TargetType="DataGrid">
                <Setter Property="Background" Value="Transparent" />
                <Setter Property="Foreground" Value="{DynamicResource TextFillColorPrimaryBrush}" />
                <Setter Property="BorderBrush" Value="Transparent" />
                <Setter Property="HeadersVisibility" Value="Column" />
                <Setter Property="HorizontalGridLinesBrush" Value="Transparent" />
                <Setter Property="VerticalGridLinesBrush" Value="Transparent" />
            </Style>

            <Style TargetType="DataGridColumnHeader">
                <Setter Property="Background" Value="{DynamicResource ControlFillColorSecondaryBrush}" />
                <Setter Property="Foreground" Value="{DynamicResource TextFillColorPrimaryBrush}" />
                <Setter Property="BorderBrush" Value="Transparent" />
                <Setter Property="FontWeight" Value="SemiBold" />
                <Setter Property="Padding" Value="8,6" />
            </Style>

            <Style TargetType="DataGridCell">
                <Setter Property="Background" Value="Transparent" />
                <Setter Property="Foreground" Value="{DynamicResource TextFillColorPrimaryBrush}" />
                <Setter Property="BorderThickness" Value="0" />
                <Setter Property="Padding" Value="8,6" />
            </Style>

            <Style BasedOn="{StaticResource DefaultTextBoxStyle}" TargetType="{x:Type TextBox}">
                <Setter Property="behavior:ClipboardInterceptor.EnableSafeClipboard" Value="True" />
            </Style>
            <Style BasedOn="{StaticResource DefaultTextBoxStyle}" TargetType="{x:Type ui:TextBox}">
                <Setter Property="behavior:ClipboardInterceptor.EnableSafeClipboard" Value="True" />
            </Style>

        </ResourceDictionary>
    </Application.Resources>
</Application>
