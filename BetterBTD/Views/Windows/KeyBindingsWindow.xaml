<ui:FluentWindow x:Class="BetterBTD.Views.Windows.KeyBindingsWindow"
                 xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                 xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                 xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
                 xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
                 xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
                 xmlns:kb="clr-namespace:BetterBTD.View.Controls.KeyBindings"
                 mc:Ignorable="d"
                 Title="{Binding WindowTitle}"
                 Width="800"
                 Height="600"
                 MinWidth="760"
                 MinHeight="520"
                 ExtendsContentIntoTitleBar="True"
                 WindowBackdropType="Mica"
                 WindowCornerPreference="Round"
                 WindowStartupLocation="CenterOwner">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <ui:TitleBar Grid.Row="0"
                     Title="{Binding WindowTitle}">
            <ui:TitleBar.Icon>
                <ui:SymbolIcon Symbol="KeyboardMouse16" />
            </ui:TitleBar.Icon>
        </ui:TitleBar>

        <Grid Grid.Row="1" Margin="42,16,42,12">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="*" />
            </Grid.RowDefinitions>

            <ui:TextBlock Grid.Row="0"
                          Margin="0,0,0,8"
                          FontWeight="SemiBold"
                          Text="{Binding PageTitle}"
                          TextWrapping="Wrap" />

            <ui:TextBlock Grid.Row="1"
                          Margin="0,0,0,8"
                          Foreground="{ui:ThemeResource TextFillColorTertiaryBrush}"
                          Text="{Binding PageSubtitle}"
                          TextWrapping="Wrap" />

            <Grid Grid.Row="2" Margin="0,0,0,8">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>

                <StackPanel Grid.Column="0">
                    <ui:TextBlock FontWeight="SemiBold" Text="{Binding ResetDefaultTitle}" />
                    <ui:TextBlock Foreground="{ui:ThemeResource TextFillColorTertiaryBrush}"
                                  Text="{Binding ResetDefaultSubtitle}"
                                  TextWrapping="Wrap" />
                </StackPanel>

                <ui:Button Grid.Column="1"
                           HorizontalAlignment="Right"
                           Command="{Binding ResetDefaultKeyBindingsCommand}"
                           Content="{Binding ResetDefaultText}" />
            </Grid>

            <ui:Border Grid.Row="3"
                       Background="{DynamicResource CardBackground}"
                       BorderBrush="{DynamicResource CardBorderBrush}"
                       BorderThickness="1"
                       CornerRadius="{DynamicResource ControlCornerRadius}">
                <Grid Margin="4">
                    <Grid Visibility="Hidden">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="170" />
                        </Grid.ColumnDefinitions>
                        <Border x:Name="TreeColumnStar" Grid.Column="0" />
                    </Grid>

                    <ui:TreeListView BorderThickness="0" ItemsSource="{Binding KeyBindingSettingModels}">
                        <ui:TreeListView.Columns>
                            <GridViewColumnCollection>
                                <ui:GridViewColumn Width="{Binding ActualWidth, ElementName=TreeColumnStar}" Header="{Binding ActionHeaderText}">
                                    <ui:GridViewColumn.CellTemplate>
                                        <DataTemplate>
                                            <ui:TreeRowExpander Content="{Binding ActionName}" />
                                        </DataTemplate>
                                    </ui:GridViewColumn.CellTemplate>
                                </ui:GridViewColumn>
                                <ui:GridViewColumn Width="170" Header="{Binding KeyBindingHeaderText}">
                                    <ui:GridViewColumn.CellTemplate>
                                        <DataTemplate>
                                            <kb:KeyBindingTextBox Padding="16,6,16,6"
                                                                  VerticalAlignment="Center"
                                                                  KeyBinding="{Binding KeyValue, Mode=TwoWay}"
                                                                  Style="{StaticResource DefaultTextBoxStyle}"
                                                                  TextAlignment="Center"
                                                                  Visibility="{Binding IsDirectory, Converter={StaticResource BooleanToVisibilityRevertConverter}}" />
                                        </DataTemplate>
                                    </ui:GridViewColumn.CellTemplate>
                                </ui:GridViewColumn>
                            </GridViewColumnCollection>
                        </ui:TreeListView.Columns>
                        <ui:TreeListView.ItemTemplate>
                            <HierarchicalDataTemplate ItemsSource="{Binding Children}" />
                        </ui:TreeListView.ItemTemplate>
                    </ui:TreeListView>
                </Grid>
            </ui:Border>
        </Grid>
    </Grid>
</ui:FluentWindow>
