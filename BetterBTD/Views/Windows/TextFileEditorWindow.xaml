<ui:FluentWindow x:Class="BetterBTD.Views.Windows.TextFileEditorWindow"
                 xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                 xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                 xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
                 xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
                 xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
                 xmlns:wv2="clr-namespace:Microsoft.Web.WebView2.Wpf;assembly=Microsoft.Web.WebView2.Wpf"
                 mc:Ignorable="d"
                 Title="{Binding WindowTitle}"
                 Width="1250"
                 Height="652"
                 MinWidth="860"
                 MinHeight="520"
                 ExtendsContentIntoTitleBar="True"
                 WindowBackdropType="Mica"
                 WindowCornerPreference="Default"
                 WindowStartupLocation="CenterOwner">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <ui:TitleBar Grid.Row="0"
                     Title="{Binding DocumentTitleText}">
            <ui:TitleBar.Icon>
                <ui:SymbolIcon Symbol="DocumentEdit24" />
            </ui:TitleBar.Icon>
        </ui:TitleBar>

        <Menu Grid.Row="1"
              Margin="0"
              Background="Transparent"
              FontSize="14">
            <ui:MenuItem Header="{Binding SaveText}">
                <MenuItem Command="{Binding SaveCommand}"
                          Header="{Binding SaveText}" />
            </ui:MenuItem>
            <ui:MenuItem Header="{Binding ReloadText}">
                <MenuItem Command="{Binding ReloadCommand}"
                          Header="{Binding ReloadText}" />
            </ui:MenuItem>
            <ui:MenuItem Header="{Binding CloseText}">
                <MenuItem Command="{Binding CloseCommand}"
                          Header="{Binding CloseText}" />
            </ui:MenuItem>
        </Menu>

        <wv2:WebView2 x:Name="EditorWebView"
                      Grid.Row="2"
                      Margin="0"
                      HorizontalAlignment="Stretch"
                      VerticalAlignment="Stretch" />

        <StatusBar Grid.Row="3"
                   MinHeight="0"
                   Margin="0"
                   Padding="8,2">
            <StatusBar.ItemsPanel>
                <ItemsPanelTemplate>
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto" />
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="Auto" />
                        </Grid.ColumnDefinitions>
                    </Grid>
                </ItemsPanelTemplate>
            </StatusBar.ItemsPanel>

            <StatusBarItem>
                <ui:SymbolIcon Foreground="{DynamicResource TextFillColorSecondaryBrush}" Symbol="DocumentEdit24" />
            </StatusBarItem>
            <StatusBarItem Grid.Column="1" Margin="6,0,0,0">
                <TextBlock Foreground="{DynamicResource TextFillColorSecondaryBrush}"
                           Text="{Binding StatusText}" />
            </StatusBarItem>
            <StatusBarItem Grid.Column="2">
                <TextBlock Foreground="{DynamicResource TextFillColorSecondaryBrush}"
                           Text="{Binding FilePath}" />
            </StatusBarItem>
        </StatusBar>
    </Grid>
</ui:FluentWindow>
