<ui:FluentWindow x:Class="BetterBTD.Views.Windows.ScriptTagEditorWindow"
                 xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                 xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                 xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
                 xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
                 xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
                 mc:Ignorable="d"
                 Title="{Binding TagEditorWindowTitle}"
                 Width="560"
                 Height="420"
                 MinWidth="500"
                 MinHeight="360"
                 ShowInTaskbar="False"
                 ExtendsContentIntoTitleBar="True"
                 WindowBackdropType="Mica"
                 WindowCornerPreference="Round"
                 WindowStartupLocation="CenterOwner"
                 PreviewKeyDown="OnPreviewKeyDown">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <ui:TitleBar Grid.Row="0"
                     Title="{Binding TagEditorWindowTitle}" />

        <Grid Grid.Row="1" Margin="20,16,20,20">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="*" />
            </Grid.RowDefinitions>

            <TextBlock Grid.Row="0"
                       Style="{StaticResource CardDescriptionTextStyle}"
                       Text="{Binding MetadataTagText}" />

            <DockPanel Grid.Row="1" Margin="0,8,0,0" LastChildFill="False">
                <Button DockPanel.Dock="Right"
                        MinWidth="96"
                        Margin="8,0,0,0"
                        Padding="12,6"
                        Command="{Binding AddScriptTagCommand}"
                        Content="{Binding MetadataTagAddText}" />
                <ComboBox Width="300"
                          DisplayMemberPath="DisplayName"
                          IsEditable="True"
                          IsTextSearchEnabled="True"
                          ItemsSource="{Binding TagOptions}"
                          SelectedItem="{Binding SelectedTagOption}"
                          Text="{Binding PendingTagInput, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" />
            </DockPanel>

            <Grid Grid.Row="2" Margin="0,10,0,0">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="*" />
                </Grid.RowDefinitions>

                <TextBlock Grid.Row="0"
                           Foreground="{DynamicResource TextFillColorSecondaryBrush}"
                           Text="{Binding MetadataTagHintText}"
                           TextWrapping="Wrap" />

                <ScrollViewer Grid.Row="1"
                              Margin="0,10,0,0"
                              VerticalScrollBarVisibility="Auto">
                    <ItemsControl ItemsSource="{Binding SelectedTagOptions}">
                        <ItemsControl.ItemsPanel>
                            <ItemsPanelTemplate>
                                <WrapPanel />
                            </ItemsPanelTemplate>
                        </ItemsControl.ItemsPanel>
                        <ItemsControl.ItemTemplate>
                            <DataTemplate>
                                <Border Margin="0,0,8,8"
                                        Padding="10,4,6,4"
                                        Background="{DynamicResource ControlFillColorSecondaryBrush}"
                                        BorderBrush="{DynamicResource ControlStrongStrokeColorDefaultBrush}"
                                        BorderThickness="1"
                                        CornerRadius="14">
                                    <StackPanel Orientation="Horizontal">
                                        <TextBlock VerticalAlignment="Center" Text="{Binding DisplayName}" />
                                        <Button Margin="8,0,0,0"
                                                Padding="0"
                                                Background="Transparent"
                                                BorderThickness="0"
                                                Command="{Binding DataContext.RemoveScriptTagCommand, RelativeSource={RelativeSource AncestorType=ui:FluentWindow}}"
                                                CommandParameter="{Binding Code}"
                                                Content="×"
                                                Cursor="Hand"
                                                FontSize="13"
                                                FontWeight="SemiBold" />
                                    </StackPanel>
                                </Border>
                            </DataTemplate>
                        </ItemsControl.ItemTemplate>
                    </ItemsControl>
                </ScrollViewer>
            </Grid>
        </Grid>
    </Grid>
</ui:FluentWindow>
